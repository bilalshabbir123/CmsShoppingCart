@model CmsShoppingCart.Models.ViewModels.Cart.CartVM

<div class="ajaxcart" style="color:white;">
    Your Car: @Model.Quantity items(s) $@Model.Price
    <br />
    @Html.ActionLink("Checkout","Index")
</div>